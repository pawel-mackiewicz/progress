# Public Assets

Replace these placeholder assets for each app:

- `favicon.svg`: browser tab icon.
- `icons/icon-192.svg`: install icon for smaller PWA surfaces.
- `icons/icon-512.svg`: install icon for larger PWA surfaces and maskable launchers.

Keep paths aligned with the PWA manifest in `vite.config.ts`.
