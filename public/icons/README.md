# PWA Icons

The template includes neutral SVG placeholders:

- `icon-192.svg`
- `icon-512.svg`

Replace them with the new app icons before publishing. Keep the file names or update `vite.config.ts` to match the new paths.
